<% if can? :change_all_results, game or (Date.yesterday() - game.gameday.date < 1) %>
  <%= render partial: "form_editable", locals: { game: game }%>
<% else %>
  <%= render partial: "form_static", locals: { game: game }%>
<% end %>